[[repos]]
repo = "local"
hooks = [
  {
    id = "lint-fix",
    name = "lint & format (auto-fix)",
    language = "system",
    entry = "sh -c 'pnpm -r --include-workspace-root check && git add -u'",
    pass_filenames = false,
  },
  {
    id = "typecheck",
    name = "typecheck",
    language = "system",
    entry = "pnpm -r --include-workspace-root typecheck",
    pass_filenames = false,
  },
  {
    id = "format-check",
    name = "format check",
    language = "system",
    entry = "pnpm -r --include-workspace-root format",
    pass_filenames = false,
  },
]
